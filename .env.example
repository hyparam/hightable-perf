# Copy this file to .env and fill in the values

# GitHub API token for authenticated requests (needed to avoid rate limits)
# Go to https://github.com/settings/personal-access-tokens and generate a token (eg. "hightable-perf")
# with no specific permission.
GITHUB_TOKEN="...your_github_token_here..."
